# Food Game Media — Netlify redirects
# v1.0 (2026-08-26)
# Repo: FoodGameMedia/fgww — commit as _redirects at the repo root (no extension)

# Feed lives where the issues live. These keep the apex URLs working for anything
# that was handed the foodgamemedia.com.au form.
/newsletter/rss.xml    https://newsletter.foodgamemedia.com.au/rss.xml     301!
/newsletter/feed.json  https://newsletter.foodgamemedia.com.au/feed.json   301!
/rss.xml               https://newsletter.foodgamemedia.com.au/rss.xml     301!
/feed.json             https://newsletter.foodgamemedia.com.au/feed.json   301!

# Pretty URL for the archive page
/newsletter            /newsletter.html                                    200

# Podcast hidden 2026-09-02. Markup is commented out in place on all six pages,
# so restoring it is one edit. This keeps the old anchor from dead-ending.
/podcast               /                                                   301

# Hard Rule 14: no instrument is ever a direct download. Forced (!) so it
# shadows diagnostic-card.pdf, which stays in the repo until the rebuilt
# card is bundled into the Worker and delivered by email only.
/diagnostic-card.pdf   /                                                   301!

# Platform pages. 200 rewrite, same pattern as /newsletter, so the pretty
# URL serves the file without a visible .html.
/matrix-mate           /matrix-mate.html                                   200
/venue-by-design       /venue-by-design.html                               200
